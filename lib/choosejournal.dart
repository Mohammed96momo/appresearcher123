import 'package:flutter/material.dart';

class ChooseJournalPage extends StatelessWidget {
  const ChooseJournalPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Container(
        child: const Center(
      child: Text("ChooseJournalPage"),
    ));
  }
}
