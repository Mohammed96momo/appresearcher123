import 'package:flutter/material.dart';

class WriteSearchPage extends StatelessWidget {
  const WriteSearchPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
        child: const Center(
      child: Text("WriteSearchPage"),
    ));
  }
}
